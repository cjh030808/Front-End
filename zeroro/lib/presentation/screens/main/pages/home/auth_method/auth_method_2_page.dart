import 'package:flutter/material.dart';

class AuthMethod2Page extends StatelessWidget {
  const AuthMethod2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('퀴즈 인증')),
      body: const Center(
        child: Text('퀴즈 인증 페이지'),
      ),
    );
  }
}
