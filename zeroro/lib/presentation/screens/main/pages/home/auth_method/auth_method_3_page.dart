import 'package:flutter/material.dart';

class AuthMethod3Page extends StatelessWidget {
  const AuthMethod3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('글 인증')),
      body: const Center(
        child: Text('글 인증 페이지'),
      ),
    );
  }
}
